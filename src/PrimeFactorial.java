public class PrimeFactorial{
    public static void main(String[] args) {
        // create an instance of the gui
        GUI gui = new GUI();

        // create instance of the controller with the gui as parameter
        Controller controller = new Controller(gui);
    }
}
